# Runtime agent dirs are generated by deploy-agent.sh
# They contain machine-specific paths and .env symlinks
# Only agent-template/ is committed (the source template)
*
!.gitignore
!agent-template/
!agent-template/**
